#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

const int MIN_N = (int) 1;
const int MAX_N = (int) 1e5;
const int MIN_K = (int) 1;
const int MAX_K = (int) 26;

int main(int argc, char* argv[]) {
  registerValidation(argc, argv);
  int n = inf.readInt(MIN_N, MAX_N, "n");
  inf.readSpace();
  int k = inf.readInt(MIN_K, MAX_K, "k");
  inf.readEoln();
  inf.readEof();
  return 0;
}
